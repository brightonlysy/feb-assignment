import React, { Component } from 'react';

const hero => () {
    return(
        left: 575,
        
    )
}
var hero = {
    left: 575,
    top: 700
};

document.onkeydown = function(e) {
    if(e.keyCode === 37) {
        //Left
        hero.left = hero.left - 10;
    }
}

export default hero